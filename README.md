# VERONA

App-programma per la settimana a Verona: apri `index.html` in un browser (anche da telefono) — nessuna installazione richiesta.

Per darla ai genitori con un link invece che un file, attiva GitHub Pages su questo repo (Settings → Pages → Deploy from branch → scegli il branch che contiene `index.html` → `/root`).
